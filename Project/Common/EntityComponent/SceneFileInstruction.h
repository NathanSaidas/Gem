#ifndef GEM_SCENE_FILE_INSTRUCTION_H
#define GEM_SCENE_FILE_INSTRUCTION_H

namespace Gem
{
	class SceneFileInstruction
	{
		//Add GameObject

		//Write[Debug]
		// fn(%s [%u] [%u]) { <instruction name> [index-ID] [parent-index-ID] }
		// foreach param
		// fn(%s = (%s)%s) { <member name> <typename> <binary data in string format> {Accepted Types, 
	};
}

#endif